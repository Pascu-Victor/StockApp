<Window
    x:Class="StockApp.Views.Pages.AnalysisWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StockApp.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d"
    Title="AnalysisWindow">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- User Info Section -->
        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock x:Name="IdTextBlock" Style="{StaticResource BaseTextBlockStyle}"/>
            <TextBlock x:Name="FirstNameTextBlock" Style="{StaticResource BaseTextBlockStyle}"/>
            <TextBlock x:Name="LastNameTextBlock" Style="{StaticResource BaseTextBlockStyle}"/>
            <TextBlock x:Name="CNPTextBlock" Style="{StaticResource BaseTextBlockStyle}"/>
            <TextBlock x:Name="EmailTextBlock" Style="{StaticResource BaseTextBlockStyle}"/>
            <TextBlock x:Name="PhoneNumberTextBlock" Style="{StaticResource BaseTextBlockStyle}"/>
        </StackPanel>

        <!-- Credit Score Plot -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10">
                <Button Content="Weekly" Click="OnWeeklyButtonClick"/>
                <Button Content="Monthly" Click="OnMonthlyButtonClick"/>
                <Button Content="Yearly" Click="OnYearlyButtonClick"/>
            </StackPanel>

            <lvc:CartesianChart x:Name="CreditScorePlotView" Grid.Row="1"/>
        </Grid>

        <!-- Activity List -->
        <Grid Grid.Row="2" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Activity History" Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0"/>
            <ListView x:Name="ActivityListView" Grid.Row="1">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding ActivityName}" Grid.Column="0"/>
                            <TextBlock Text="{Binding LastModifiedAmount}" Grid.Column="1" Margin="10,0"/>
                            <TextBlock Text="{Binding CreatedAt, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter='MM/dd/yyyy'}" Grid.Column="2"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Window>
